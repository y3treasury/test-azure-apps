server:
  port: 8091

eureka:
  client:
    serviceUrl:
      defaultZone: https://treasury-discovery.azurewebsites.net
      
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"